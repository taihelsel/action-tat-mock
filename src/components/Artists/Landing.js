import React, { Component } from 'react';
import '../.././App.css';

class Landing extends Component {
  render() {
    return (
      <section id="artists-landing">
        landing page
      </section>
    );
  }
}

export default Landing;
